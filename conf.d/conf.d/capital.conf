<VirtualHost *:80>

    ServerName  worthycreditcapitals.co.ke
    DocumentRoot /var/www/html/worth/
#RewriteEngine on
#RewriteCond %{SERVER_NAME} =sna.co.ke
#RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
RewriteEngine on
RewriteCond %{SERVER_NAME} =sna.co.ke
RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>
