<VirtualHost *:80>

    ServerName  levetot.co.ke
    DocumentRoot /var/www/html/levetot/
 # Redirect / https://levetot.co.ke
#RewriteEngine on
#RewriteCond %{SERVER_NAME} =sna.co.ke
#RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
#RewriteEngine on
#RewriteCond %{SERVER_NAME} =sna.co.ke
#RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
#RewriteEngine on
#RewriteCond %{SERVER_NAME} =levetot.co.ke
#RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>
